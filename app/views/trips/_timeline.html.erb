<% if @trips.present? %>
  <div class="text-white flex">
    <div class="relative flex-1 flex flex-col gap-1 pl-1 pr-5">
      <div class="absolute top-24 bottom-0 left-[58px] w-px bg-white"></div>

      <% @trips.each do |trip| %>
        <% if trip.id != @trips[0].id%>
          <%= render "timeline_card", trip: trip %>
        <% end %>
      <% end %>

    </div>
  </div>
<% end %>
